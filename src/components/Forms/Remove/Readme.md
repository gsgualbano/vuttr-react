#### Formulário para remoção de ferramentas.

Exemplo:

```js
const tool = {
  id: 1,
  title: "Google",
  description: "Search Website",
  link: "https://google.com",
  tags: ["search", "site", "google"]
};

const setShowModal = () => {};

<Remove tool={tool} setShowModal={setShowModal} />;
```
